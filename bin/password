#!/usr/bin/env bash

# Generate a random password of a given length (default: 16)
# Uses openssl for uniform character distribution
length="${1:-16}"
password=$(openssl rand -base64 48 | LC_ALL=C tr -dc 'A-Za-z0-9!@#$%^&*()_+=' | head -c "$length")
echo "$password" | pbcopy
echo "$password"